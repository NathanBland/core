const config = {
  twitter: {
    TWITTER_CONSUMER_KEY: '', // you need to add this yourself.
    TWITTER_CONSUMER_SECRET: '', // same here
    callbackURL: 'https://core.api.local/api/auth/twitter/callback'
  }
}

module.exports = config